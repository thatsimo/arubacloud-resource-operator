apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: vpcs.arubacloud.com
  annotations:
    controller-gen.kubebuilder.io/version: v0.18.0
  labels:
  {{- include "crd.labels" . | nindent 4 }}
spec:
  group: arubacloud.com
  names:
    kind: Vpc
    listKind: VpcList
    plural: vpcs
    shortNames:
    - vpc
    singular: vpc
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.resourceID
      name: Resource ID
      type: string
    - jsonPath: .status.message
      name: Message
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Vpc is the Schema for the vpcs API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: VpcSpec defines the desired state of Vpc.
            properties:
              location:
                description: Location specifies the location for the vpc
                properties:
                  value:
                    description: Value is the location identifier (e.g., "ITBG-Bergamo")
                    type: string
                required:
                - value
                type: object
              projectReference:
                description: ProjectReference references the Project that owns this
                  vpc
                properties:
                  name:
                    description: Name is the name of the referenced resource
                    type: string
                  namespace:
                    description: Namespace is the namespace of the referenced resource
                    type: string
                required:
                - name
                - namespace
                type: object
              tags:
                description: Tags are labels associated with the vpc
                items:
                  type: string
                type: array
              tenant:
                description: Tenant is the owning account/tenant of this vpc
                type: string
            required:
            - location
            - projectReference
            - tenant
            type: object
          status:
            description: VpcStatus defines the observed state of Vpc.
            properties:
              conditions:
                description: Conditions represent the latest available observations
                  of the Resource state
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              message:
                description: Message provides human-readable information about the current
                  state
                type: string
              observedGeneration:
                description: ObservedGeneration is the most recent generation observed
                  by the controller
                format: int64
                type: integer
              phase:
                description: Phase represents the current phase of the resource
                type: string
              phaseStartTime:
                description: PhaseStartTime tracks when the current phase started
                format: date-time
                type: string
              projectID:
                description: ProjectID is the project ID where this vpc is created
                type: string
              resourceID:
                description: ResourceID is the unique identifier of the resource in
                  the remote system
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
